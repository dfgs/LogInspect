<UserControl x:Class="LogInspect.Views.RateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:LogInspect.Views"
             mc:Ignorable="d" 
			 d:DesignHeight="40" d:DesignWidth="800" Background="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}">
	<Border BorderThickness="1,1,0,0" BorderBrush="#30000000">
		<Border.ToolTip>
			<StackPanel>
				<TextBlock Text="{Binding Rate,FallbackValue=NA,StringFormat='Rate {0}/s'}"/>
				<TextBlock Text="{Binding MaxRate,FallbackValue=NA,StringFormat='Max rate {0}/s'}"/>
			</StackPanel>
		</Border.ToolTip>
		<views:ProgressPanel>
			<Border Background="Green" views:ProgressPanel.Value="{Binding Rate}" views:ProgressPanel.Maximum="{Binding MaxRate,FallbackValue=100}" />
		</views:ProgressPanel>
	</Border>
</UserControl>
