<UserControl x:Class="LogInspect.Views.ModuleStatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:LogInspect.Views"
             mc:Ignorable="d" x:Name="root"
              d:DesignWidth="800">
    <StackPanel Orientation="Horizontal">
		<TextBlock VerticalAlignment="Center" Text="{Binding ElementName=root,Path=Header,FallbackValue=Header:}"/>
		<TextBlock VerticalAlignment="Center" Text="{Binding ProceededCount,FallbackValue=NA}" Margin="8,0,0,0"/>
		<views:RateView Width="24" Height="8" VerticalAlignment="Center" Margin="8,0,0,0"/>
	</StackPanel>
</UserControl>
