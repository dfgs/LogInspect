<UserControl x:Class="LogInspect.Views.HeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:LogInspect.Views"
             mc:Ignorable="d" 
             d:DesignHeight="16" d:DesignWidth="800">
	
    <ItemsControl ItemsSource="{Binding Columns,FallbackValue= {x:Static views:HeaderView.TestHeaders}}">
		<ItemsControl.ItemsPanel>
			<ItemsPanelTemplate>
				<StackPanel Orientation="Horizontal"/>
			</ItemsPanelTemplate>
		</ItemsControl.ItemsPanel>
		<ItemsControl.ItemTemplate>
			<DataTemplate>
				<StackPanel Orientation="Horizontal" Width="{Binding Width}">
					<TextBlock Text="{Binding Text}"/>
					<Thumb Width="4" Background="Red" Cursor="SizeWE" DragDelta="Thumb_DragDelta"/>
				</StackPanel>
			</DataTemplate>
		</ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
