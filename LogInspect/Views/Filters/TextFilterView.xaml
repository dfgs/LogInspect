<UserControl x:Class="LogInspect.Views.Filters.TextFilterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
 			 xmlns:filters="clr-namespace:LogInspect.Models.Filters"
            xmlns:local="clr-namespace:LogInspect.Views.Filters"
             mc:Ignorable="d" 
              d:DesignWidth="200">
	<StackPanel>
		<StackPanel Margin="4">
			<TextBlock Text="Condition:" FontWeight="Bold"/>
			<ComboBox x:Name="conditionComboBox" Text="{Binding Condition}" ItemsSource="{x:Static filters:TextFilter.Conditions}"/>
		</StackPanel>
		<StackPanel Margin="4">
			<TextBlock Text="Value:" FontWeight="Bold"/>
			<TextBox Text="{Binding Value}" />
		</StackPanel>
	</StackPanel>
</UserControl>
