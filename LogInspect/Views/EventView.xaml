<UserControl x:Class="LogInspect.Views.EventView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:LogInspect.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<ListView ItemsSource="{Binding}" ScrollViewer.IsDeferredScrollingEnabled="True">
		<ListView.View>
			<GridView>
				<GridView.Columns>
					<GridViewColumn Header="test" Width="300" DisplayMemberBinding="{Binding Log}"/>
				</GridView.Columns>
			</GridView>
		</ListView.View>
		<ListView.ItemsPanel>
			<ItemsPanelTemplate>
				<views:EventPanel />
			</ItemsPanelTemplate>
		</ListView.ItemsPanel>
	</ListView>
	
</UserControl>
