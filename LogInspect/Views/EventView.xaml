<UserControl x:Class="LogInspect.Views.EventView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:LogInspect.Views"
			 xmlns:models="clr-namespace:LogInspectLib;assembly=LogInspectLib"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

	<UserControl.Resources>
		<DataTemplate DataType="{x:Type models:Property}">
			<TextBlock Text="{Binding Value}"/>
		</DataTemplate>
		<DataTemplate x:Key="propertyDataTemplate">
			<TextBlock Text="{Binding Value}"/>
		</DataTemplate>
	</UserControl.Resources>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="7"/>
			<RowDefinition Height="150"/>
		</Grid.RowDefinitions>
		<DockPanel Grid.Row="0">
			<ScrollBar DockPanel.Dock="Right" Value="{Binding Position}" Maximum="{Binding Count}" SmallChange="1" LargeChange="{Binding PageSize}" ViewportSize="{Binding PageSize}"/>
			<ListView x:Name="listView" ItemsSource="{Binding}" ScrollViewer.VerticalScrollBarVisibility="Hidden" PreviewMouseWheel="ListView_MouseWheel" PreviewKeyDown="ListView_PreviewKeyDown" >
				<ListView.ItemContainerStyle>
					<Style TargetType="{x:Type ListViewItem}">
						<Setter Property="IsSelected" Value="{Binding IsSelected}"/>
						<Setter Property="Height" Value="24"/>
						<Setter Property="Margin" Value="0"/>
						<Setter Property="Background" Value="{Binding Background}"/>
					</Style>
				</ListView.ItemContainerStyle>
				<ListView.View>
					<GridView>
						<GridViewColumn Header="#" Width="50" DisplayMemberBinding="{Binding LineIndex}"/>
						<GridViewColumn Header="Date" Width="100" DisplayMemberBinding="{Binding Date}"/>
						<GridViewColumn Header="Severity" Width="100" DisplayMemberBinding="{Binding Severity}"/>
						<GridViewColumn Header="Thread" Width="150" DisplayMemberBinding="{Binding Thread}"/>
						<GridViewColumn Header="Message" Width="500" DisplayMemberBinding="{Binding Message}"/>
					</GridView>
				</ListView.View>
				<ListView.ItemsPanel>
					<ItemsPanelTemplate>
						<StackPanel views:ItemsEvaluator.LogFileViewModel="{Binding}" views:ItemsEvaluator.ItemHeight="24"/>
					</ItemsPanelTemplate>
				</ListView.ItemsPanel>
			</ListView>
		</DockPanel>
		<GridSplitter Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
		<ListBox Grid.Row="2" ItemsSource="{Binding ElementName=listView,Path=SelectedItem.Lines}"/>
	</Grid>
</UserControl>
