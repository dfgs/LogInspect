<UserControl x:Class="LogInspect.Views.EventView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:LogInspect.Views"
			 xmlns:propertyVM="clr-namespace:LogInspect.ViewModels.Properties"
			 xmlns:models="clr-namespace:LogInspectLib;assembly=LogInspectLib"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

	<UserControl.Resources>
		<views:NotValueConverter x:Key="notValueConverter"/>

		<views:BooleanToVisibiltyConverter x:Key="booleanToVisibilityConverter"/>

		<Style x:Key="columnHeaderStyle" TargetType="{x:Type GridViewColumnHeader}">
			<Setter Property="HorizontalContentAlignment" Value="Stretch"/>
		</Style>
		
		<Style x:Key="filterButtonStyle" TargetType="{x:Type Border}">
			<Setter Property="Visibility" Value="Collapsed"/>
			<Style.Triggers>
				<MultiDataTrigger>
					<MultiDataTrigger.Conditions>
						<Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=GridViewColumnHeader} ,Path=IsMouseOver}" Value="true"/>
						<Condition Binding="{Binding Path=AllowsFilter}" Value="true"/>
					</MultiDataTrigger.Conditions>

					<Setter Property="Visibility" Value="Visible"/>
				</MultiDataTrigger>
				<DataTrigger Binding="{Binding HasFilter}" Value="true">
					<Setter Property="Visibility" Value="Visible"/>
				</DataTrigger>
				
			</Style.Triggers>
		</Style>
		
		<DataTemplate x:Key="columnHeaderTemplate">
			<DockPanel>
				<Image DockPanel.Dock="Left" Source="{Binding ImageSource}" Visibility="{Binding ImageVisibility}" Stretch="Uniform" Width="12" Height="12"/>
				<Border DockPanel.Dock="Right" Style="{StaticResource filterButtonStyle}">
					<Image Source="/LogInspect;component/Images/Filter_16x.png" Stretch="Uniform" Width="12" Height="12"/>
				</Border>
				<TextBlock Text="{Binding Description}" HorizontalAlignment="Center"/>
			</DockPanel>
		</DataTemplate>
		
		
		
	
		
		<DataTemplate DataType="{x:Type propertyVM:BookMarkPropertyViewModel}">
			<Image Source="/LogInspect;component/Images/Bookmark_16x.png" Stretch="None" HorizontalAlignment="Center" Visibility="{Binding Event.IsBookMarked,Converter={StaticResource booleanToVisibilityConverter}}"/>
		</DataTemplate>
		
		<DataTemplate DataType="{x:Type propertyVM:LinePropertyViewModel}">
			<TextBlock  Text="{Binding Value}" FontWeight="Bold" TextAlignment="{Binding Alignment}" Foreground="Gray"/>
		</DataTemplate>

		<DataTemplate DataType="{x:Type propertyVM:TimeStampPropertyViewModel}">
			<TextBlock Text="{Binding Value}" TextAlignment="{Binding Alignment}" TextTrimming="CharacterEllipsis" Foreground="Black"/>
		</DataTemplate>
		
		<DataTemplate DataType="{x:Type propertyVM:SeverityPropertyViewModel}">
			<DockPanel>
				<Border DockPanel.Dock="Left" Width="4" Background="{Binding Background}" VerticalAlignment="Stretch"/>
				<TextBlock  Text="{Binding Value}" Foreground="{Binding Foreground}" TextAlignment="{Binding Alignment}" TextTrimming="CharacterEllipsis"/>
			</DockPanel>
		</DataTemplate>
		
		<DataTemplate DataType="{x:Type propertyVM:TextPropertyViewModel}">
			<TextBlock views:Inlines.InlineSource="{Binding Inlines}" TextTrimming="CharacterEllipsis"/>
		</DataTemplate>
		<!-- -->

	</UserControl.Resources>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="7"/>
			<RowDefinition Height="150"/>
		</Grid.RowDefinitions>


		<ListView Grid.Row="0" ItemsSource="{Binding Events}" ScrollViewer.VerticalScrollBarVisibility="Auto"  ScrollViewer.HorizontalScrollBarVisibility="Auto" 
					 SelectedItem="{Binding Events.SelectedItem}" 
				  SelectionChanged="ListView_Selected"  GridViewColumnHeader.Click="ListView_Click"
				 VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" ScrollViewer.IsDeferredScrollingEnabled="True">
			<ListView.Style>
				<Style TargetType="{x:Type ListView}">
					<Setter Property="IsEnabled" Value="False"/>
					<Style.Triggers>
						<DataTrigger Binding="{Binding Status}" Value="Idle">
							<Setter Property="IsEnabled" Value="True"/>
						</DataTrigger>
					</Style.Triggers>
				</Style>
			</ListView.Style>
			<ListView.ItemContainerStyle>
				<Style TargetType="{x:Type ListViewItem}">
					<Setter Property="HorizontalContentAlignment" Value="Stretch" />
					<Setter Property="Margin" Value="0"/>
					<Setter Property="Padding" Value="0"/>
					<!--Setter Property="Background" Value="{Binding Background}"/-->
				</Style>
			</ListView.ItemContainerStyle>
			<ListView.View>
				<GridView   views:GridViewColumns.ColumnsSource="{Binding Columns}" ColumnHeaderTemplate="{StaticResource columnHeaderTemplate}" ColumnHeaderContainerStyle="{StaticResource columnHeaderStyle}"  >
					
				</GridView>
			</ListView.View>
		</ListView>
		
		
		<GridSplitter  Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
		<TabControl Grid.Row="2" DataContext="{Binding Events.SelectedItem}">
			<TabItem Header="Event details">
				<views:EventDetailsView/>
			</TabItem>
			<TabItem Header="Raw data">
				<TextBox   IsReadOnly="True"  AcceptsReturn="True" Text="{Binding Lines,Mode=OneWay}"/>
			</TabItem>
			<TabItem Header="Inlines">
				<ItemsControl ItemsSource="{Binding [Message].Inlines}">
					<ItemsControl.ItemTemplate>
						<DataTemplate>
							<TextBlock Text="{Binding Value}"/>
						</DataTemplate>
					</ItemsControl.ItemTemplate>
				</ItemsControl>
			</TabItem>
		</TabControl>
	</Grid>
</UserControl>
