<UserControl x:Class="LogInspect.Views.EventView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:LogInspect.Views"
			 xmlns:vlbLib="clr-namespace:VirtualListBoxLib;assembly=VirtualListBoxLib"
			 xmlns:models="clr-namespace:LogInspectLib;assembly=LogInspectLib"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

	<UserControl.Resources>
		<!--views:NotValueConverter x:Key="notValueConverter"/-->
		<DataTemplate DataType="{x:Type models:Property}">
			<TextBlock Text="{Binding Value}"/>
		</DataTemplate>
		
		
		<DataTemplate x:Key="propertyDataTemplate">
			<TextBlock Width="{Binding Column.Width}" Text="{Binding Value}" TextAlignment="{Binding Column.Alignment}" />
		</DataTemplate>

		<DataTemplate x:Key="eventDataTemplate">
			<ItemsControl ItemsSource="{Binding Properties}" ItemTemplate="{StaticResource propertyDataTemplate}">
				<ItemsControl.ItemsPanel>
					<ItemsPanelTemplate>
						<StackPanel Orientation="Horizontal"/>
					</ItemsPanelTemplate>
				</ItemsControl.ItemsPanel>
			</ItemsControl>
		</DataTemplate>

		<Style TargetType="{x:Type vlbLib:VirtualListBoxItem}">
			<Setter Property="Background" Value="{Binding Background}"/>
		</Style>
	</UserControl.Resources>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="7"/>
			<RowDefinition Height="150"/>
		</Grid.RowDefinitions>
		

		<views:ColumnsView Grid.Row="0"  DataContext="{Binding Columns}" HorizontalOffset="{Binding ElementName=eventPanel,Path=HorizontalOffset}" />

		<vlbLib:VirtualListBox Grid.Row="1" VirtualCollection="{Binding}" ItemsCount="{Binding  EventFilterer.IndexedEventsCount}"  ExtentWidth="{Binding Columns.TotalWidth}" ItemTemplate="{StaticResource eventDataTemplate}"/>
		<!--ScrollViewer  Grid.Row="1" CanContentScroll="True"  VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Background="{x:Static views:Options.EventBackground}" IsEnabled="{Binding IsWorking,Converter={StaticResource notValueConverter}}">
			<views:EventPanel x:Name="eventPanel"  ItemHeight="20" ItemsSource="{Binding}" ItemsCount="{Binding EventFilterer.IndexedEventsCount}" Columns="{Binding Columns}"  ExtentWidth="{Binding Columns.TotalWidth}" 
							  SelectedItem="{Binding SelectedItem,Mode=TwoWay}"/>
		</-->
		<GridSplitter  Grid.Row="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
		<TextBox  Grid.Row="3" IsReadOnly="True"  AcceptsReturn="True" Text="{Binding SelectedItem.Lines,Mode=OneWay}" FontFamily="Helvetica" FontSize="16"/>
	</Grid>
</UserControl>
